# Optional. If left blank, the Research Summary Generator (Section 8 of
# the build plan) falls back to a templated summary — the system runs
# fully end-to-end with zero external API calls either way.
ANTHROPIC_API_KEY=

# Only needed if running the backend outside Docker Compose.
DATABASE_URL=postgresql://evidenceiq:evidenceiq@localhost:5432/evidenceiq
